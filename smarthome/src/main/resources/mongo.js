/*show dbs;
use home;
db.createCollection("measure");
show collections;
db.createUser(
    {
        user:"alex",
        pwd:"pwd",
        roles:[
            {role:"readWrite",db:"home"}
        ]
    } );
db.runCommand({connectionStatus : 1})
*/
